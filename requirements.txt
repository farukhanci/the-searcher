# Direct dependencies only; everything else comes in with these.
httpx>=0.28
trafilatura>=2.2
fastapi>=0.141
uvicorn>=0.52

# Optional: without it, PDFs are skipped and the rest still works.
pypdf>=6.0
